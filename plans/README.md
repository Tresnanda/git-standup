# Implementation Plans

Generated by the `improve` skill on 2026-06-24 at commit `caff6db`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Fix custom provider key environment setup | P1 | S | - | DONE |
| 002 | Bind checkpoint writes to the fetched report window | P1 | M | - | DONE |
| 003 | Escape git and GitHub controlled Markdown in derived reports | P1 | S | - | DONE |
| 004 | Surface output budget truncation in raw reports | P2 | S | - | DONE |
| 005 | Add a dependency vulnerability audit gate | P2 | S | - | DONE |
| 006 | Add a `doctor` command for environment diagnostics | P3 | M | - | DONE |
| 007 | Design saved report profiles | P3 | M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 003 should land before adding more Markdown report surfaces, but it does not
  block 001, 002, 004, or 005.
- 006 and 007 are direction work. They can wait until the P1/P2 fixes land.
- 007 is intentionally a design spike, not a full profile implementation.

## Verification baseline from audit

- `rtk .venv/bin/ruff check .` passed with `All checks passed!`.
- `rtk .venv/bin/pytest` passed with `225 passed`.
- `python -m build` was not run during the read-only audit because it writes
  package artifacts.
- `rtk .venv/bin/python -m pip_audit` failed because `pip-audit` is not installed;
  plan 005 addresses that.

## Findings considered and rejected

- Generated artifacts under `dist/`, `src/git_standup.egg-info/`, caches, and
  `__pycache__` directories: ignored and untracked, so not worth planning.
- Shell command injection in Git/GitHub subprocess calls: rejected; audited call
  sites use argument arrays and static provider choices where it matters.
- Secret storage in `config.toml`: rejected as already covered; config parser
  rejects secret-like fields and tests assert API keys are not written.
